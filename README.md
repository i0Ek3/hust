# hust

> hi rust.

## Install & Run Rust

- `brew install rust` // for macOS
- `rustc xxx.rs` // compile rust source
- `cargo new xxx` // create a new project
- `cargo build`
- `cargo run`

## Credit

- runboo.com
